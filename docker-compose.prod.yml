name: skybooker

x-common-java-env: &common-java-env
  EUREKA_DEFAULT_ZONE: http://eureka-server:8761/eureka/
  JAVA_TOOL_OPTIONS: -XX:MaxRAMPercentage=50.0 -XX:InitialRAMPercentage=5.0 -XX:+UseSerialGC

x-default-logging: &default-logging
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"

services:
  mysql:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: auth_db
    volumes:
      - mysql_data:/var/lib/mysql
      - ./docker/mysql/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -proot"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 20s
    logging: *default-logging

  rabbitmq:
    image: rabbitmq:3.13-management
    restart: unless-stopped
    ports:
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    logging: *default-logging

  eureka-server:
    image: akash7354/skybooker-eureka-server:${DOCKER_IMAGE_TAG:-latest}
    restart: unless-stopped
    environment:
      JAVA_TOOL_OPTIONS: -XX:MaxRAMPercentage=50.0 -XX:InitialRAMPercentage=5.0 -XX:+UseSerialGC
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8761/actuator/health | grep -q \"UP\""]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 40s
    logging: *default-logging

  api-gateway:
    image: akash7354/skybooker-api-gateway:${DOCKER_IMAGE_TAG:-latest}
    restart: unless-stopped
    environment:
      <<: *common-java-env
      CORS_ALLOWED_ORIGIN: ${CORS_ALLOWED_ORIGIN:-https://52-20-59-244.nip.io}
    depends_on:
      eureka-server:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/actuator/health | grep -q \"UP\""]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 40s
    logging: *default-logging

  admin-server:
    image: akash7354/skybooker-admin-server:${DOCKER_IMAGE_TAG:-latest}
    restart: unless-stopped
    environment:
      <<: *common-java-env
      AUTH_SERVICE_BASE_URL: http://api-gateway:8080
      BOOKING_SERVICE_BASE_URL: http://api-gateway:8080
      PAYMENT_SERVICE_BASE_URL: http://api-gateway:8080
      AIRLINE_AIRPORT_SERVICE_BASE_URL: http://api-gateway:8080
      FLIGHT_SERVICE_BASE_URL: http://api-gateway:8080
      PASSENGER_SERVICE_BASE_URL: http://api-gateway:8080
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: guest
      RABBITMQ_PASSWORD: guest
    depends_on:
      eureka-server:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      api-gateway:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8090/actuator/health | grep -q \"UP\""]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 45s
    logging: *default-logging

  auth-service:
    image: akash7354/skybooker-auth-service:${DOCKER_IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file:
      - ./.env
    environment:
      <<: *common-java-env
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_DB: auth_db
      MYSQL_USER: root
      MYSQL_PASSWORD: root
      AIRLINE_AIRPORT_SERVICE_BASE_URL: http://api-gateway:8080
    depends_on:
      eureka-server:
        condition: service_healthy
      mysql:
        condition: service_healthy
      api-gateway:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8081/actuator/health | grep -q \"UP\""]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 45s
    logging: *default-logging

  flight-service:
    image: akash7354/skybooker-flight-service:${DOCKER_IMAGE_TAG:-latest}
    restart: unless-stopped
    environment:
      <<: *common-java-env
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_DB: flight_db
      MYSQL_USER: root
      MYSQL_PASSWORD: root
      FLIGHT_UPSTREAM_AIRLINE_AIRPORT_BASE_URL: http://airline-airport-service:8089
      FLIGHT_UPSTREAM_BOOKING_BASE_URL: http://booking-service:8084
      APP_SEED_DATA: ${APP_SEED_DATA:-false}
    depends_on:
      eureka-server:
        condition: service_healthy
      mysql:
        condition: service_healthy
      airline-airport-service:
        condition: service_healthy
      api-gateway:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8082/actuator/health | grep -q \"UP\""]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 45s
    logging: *default-logging

  seat-service:
    image: akash7354/skybooker-seat-service:${DOCKER_IMAGE_TAG:-latest}
    restart: unless-stopped
    environment:
      <<: *common-java-env
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_DB: seat_db
      MYSQL_USER: root
      MYSQL_PASSWORD: root
    depends_on:
      eureka-server:
        condition: service_healthy
      mysql:
        condition: service_healthy
      api-gateway:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8085/actuator/health | grep -q \"UP\""]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 45s
    logging: *default-logging

  booking-service:
    image: akash7354/skybooker-booking-service:${DOCKER_IMAGE_TAG:-latest}
    restart: unless-stopped
    environment:
      <<: *common-java-env
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_DB: booking_db
      MYSQL_USER: root
      MYSQL_PASSWORD: root
      FLIGHT_SERVICE_BASE_URL: http://api-gateway:8080
      SEAT_SERVICE_BASE_URL: http://api-gateway:8080
      PAYMENT_SERVICE_BASE_URL: http://api-gateway:8080
      PAYMENT_INTEGRATION_ENABLED: "true"
      RABBITMQ_HOST: rabbitmq
    depends_on:
      eureka-server:
        condition: service_healthy
      mysql:
        condition: service_healthy
      api-gateway:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      flight-service:
        condition: service_healthy
      seat-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8084/actuator/health | grep -q \"UP\""]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 45s
    logging: *default-logging

  passenger-service:
    image: akash7354/skybooker-passenger-service:${DOCKER_IMAGE_TAG:-latest}
    restart: unless-stopped
    environment:
      <<: *common-java-env
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_DB: passenger_db
      MYSQL_USER: root
      MYSQL_PASSWORD: root
    depends_on:
      eureka-server:
        condition: service_healthy
      mysql:
        condition: service_healthy
      api-gateway:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8086/actuator/health | grep -q \"UP\""]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 45s
    logging: *default-logging

  payment-service:
    image: akash7354/skybooker-payment-service:${DOCKER_IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file:
      - ./.env
    environment:
      <<: *common-java-env
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_DB: payment_db
      MYSQL_USER: root
      MYSQL_PASSWORD: root
      BOOKING_SERVICE_BASE_URL: http://api-gateway:8080
      RABBITMQ_HOST: rabbitmq
    depends_on:
      eureka-server:
        condition: service_healthy
      mysql:
        condition: service_healthy
      api-gateway:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8087/actuator/health | grep -q \"UP\""]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 45s
    logging: *default-logging

  notification-service:
    image: akash7354/skybooker-notification-service:${DOCKER_IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file:
      - ./.env
    environment:
      <<: *common-java-env
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_DB: notification_db
      MYSQL_USER: root
      MYSQL_PASSWORD: root
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: guest
      RABBITMQ_PASSWORD: guest
      BOOKING_SERVICE_BASE_URL: http://api-gateway:8080
      AUTH_SERVICE_BASE_URL: http://api-gateway:8080
      FLIGHT_SERVICE_BASE_URL: http://api-gateway:8080
      MAIL_HOST: ${MAIL_HOST:-smtp.gmail.com}
      MAIL_PORT: ${MAIL_PORT:-587}
      MAIL_USERNAME: ${MAIL_USERNAME:-}
      MAIL_PASSWORD: ${MAIL_PASSWORD:-}
      MAIL_SMTP_AUTH: ${MAIL_SMTP_AUTH:-true}
      MAIL_SMTP_STARTTLS: ${MAIL_SMTP_STARTTLS:-true}
      NOTIFICATION_EMAIL_FROM: ${NOTIFICATION_EMAIL_FROM:-noreply@skybooker.com}
    depends_on:
      eureka-server:
        condition: service_healthy
      mysql:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      api-gateway:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8088/actuator/health | grep -q \"UP\""]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 45s
    logging: *default-logging

  airline-airport-service:
    image: akash7354/skybooker-airline-airport-service:${DOCKER_IMAGE_TAG:-latest}
    restart: unless-stopped
    environment:
      <<: *common-java-env
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_DB: airline_airport_db
      MYSQL_USER: root
      MYSQL_PASSWORD: root
    depends_on:
      eureka-server:
        condition: service_healthy
      mysql:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8089/actuator/health | grep -q \"UP\""]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 40s
    logging: *default-logging

  frontend:
    image: akash7354/skybooker-frontend:${DOCKER_IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file:
      - ./.env
    environment:
      API_BASE_URL: ${FRONTEND_API_BASE_URL:-https://52-20-59-244.nip.io/api}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
    depends_on:
      api-gateway:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 25s
    logging: *default-logging

  reverse-proxy:
    image: caddy:2.9
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      SITE_DOMAIN: ${SITE_DOMAIN:-52-20-59-244.nip.io}
      ACME_EMAIL: ${ACME_EMAIL:-}
    depends_on:
      api-gateway:
        condition: service_healthy
      frontend:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 15s
    volumes:
      - ./docker/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    logging: *default-logging

volumes:
  mysql_data:
  rabbitmq_data:
  caddy_data:
  caddy_config:
